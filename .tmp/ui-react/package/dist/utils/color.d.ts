import { SolvedTheme } from '../styles/Themes';
export declare const readableColor: (color: string, theme: SolvedTheme) => string;
export declare const computeHoverColor: (backgroundColor: string) => string;
