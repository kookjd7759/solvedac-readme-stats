import { SolvedTheme } from '../styles';
export declare const cardHoverTemplate: {
    backgroundColor: (theme: SolvedTheme) => string;
    textColor: (theme: SolvedTheme) => string;
    hoverBackgroundColor: (theme: SolvedTheme) => string;
    hoverTextColor: (theme: SolvedTheme) => string;
};
export declare const transparentHoverTemplate: {
    backgroundColor: (theme: SolvedTheme) => string;
    textColor: (theme: SolvedTheme) => string;
    hoverBackgroundColor: (theme: SolvedTheme) => string;
    hoverTextColor: (theme: SolvedTheme) => string;
};
